<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chatterbox</title>
    <link rel="stylesheet" href = "style.css">
</head>
<body>
    <h3 class="head">Welcome to Chatterbox!</h3>
    <form method = "GET" action="/chats/new" >
        <button class="button">Wanna Chat?</button>
    </form>
    <% for(let chat of chats) { %>
        <div class = "chat">
            <div class= "msg">
                <p><b>From : </b><%= chat.from %></p>
                <p><b>Message : </b><%= chat.msg %></p>
                <form method="GET" action="chats/<%=chat._id %>/edit">
                    <button>Edit your Message!</button>
                </form>
                <p><b>To : </b><%= chat.to %></p>
            </div>
            <p><%= chat.created_at.toString().split(" ")[4] %></p>
            <p><%= chat.created_at.toString().split(" ").slice(0,4).join(" ")%></p>
            <p><form method= "POST" action = "/chats/<%=chat._id%>?_method=DELETE">
                <button>Delete this chat?</button>
            </form></p>
        </div>
        <br> <br>
   <% } %>
</body>
</html>

